import styled from "styled-components";

const Button = styled.button`
  padding: 1rem 2rem;
  color: black;
  background-color: rgb(var(--accent-color2), 50%);
  border: 2px solid black;
  margin-right: 2rem;
  font-weight: 600;
  cursor: pointer;
`;

export default Button;
